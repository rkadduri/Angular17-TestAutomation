import { DigitSpacingPipe } from './digit-spacing.pipe';

describe('DigitSpacingPipe', () => {
  it('create an instance', () => {
    const pipe = new DigitSpacingPipe();
    expect(pipe).toBeTruthy();
  });
});
